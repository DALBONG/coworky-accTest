spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test_coworky
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: dlwlstjd1

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        show_sql: true

logging:
  level:
    org.hibernate.type.descriptor.sql: trace
    org.hibernate.SQL: debug # SQL 쿼리 자체 출력